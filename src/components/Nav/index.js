import React from "react";
import { capitalizeFirstLetter } from "../../utils/helpers";

function Nav(props) {

    return (
        <header>            
        </header>
    );
}

export default Nav;